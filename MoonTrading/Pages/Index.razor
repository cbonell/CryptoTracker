@page "/d"
@inject ICryptoFacilitiesData cfd
@inject ICoinGeckoData cdd
@inject IJSRuntime js

<PageTitle>Index</PageTitle>

@code {

    protected override async Task OnInitializedAsync()
    {
        //await cfd.GetOHLCPairs("btc");
        //await MoonTrading.DataAccess.Data.CryptoWatchData.GetOHLCPairs("btc", DateTimeOffset.UtcNow.AddDays(-7));
    }
}
